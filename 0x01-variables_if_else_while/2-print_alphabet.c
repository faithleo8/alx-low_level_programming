#include <stdio.h>
/**
 * main - prints the alphabet in lowercase adn then in uppercase
 *
 *Return: Always 0(success)
 */
int main(void)
{
int x;

for (x = 'a'; x <= 'z'; x++)

putchar(x);

putchar('\n');

return (0);
}
